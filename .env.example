POSTGRES_USER=pokeprism
POSTGRES_PASSWORD=changeme_postgres
NEO4J_USER=neo4j
NEO4J_PASSWORD=changeme_neo4j
OLLAMA_PLAYER_MODEL=qwen3.5:9b-q4_K_M
OLLAMA_COACH_MODEL=gemma4-e4b:q6_K
OLLAMA_EMBED_MODEL=nomic-embed-text
TCGDEX_BASE_URL=https://api.tcgdex.net/v2/en
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Observed Play Memory — Coach advisory injection (Phase 6.1)
# Default is false (off). Uncomment to enable locally.
# Both backend and celery-worker must see this value; docker-compose.override.yml
# reads it automatically via ${OBSERVED_PLAY_MEMORY_ENABLED:-false}.
# OBSERVED_PLAY_MEMORY_ENABLED=true
